# Debug Commands
# logging.level.root=DEBUG


# Application Commands
spring.application.name=maple

# ============================================================
# DEFAULT (LOCAL DEV) - H2 in-file database
# For production, set SPRING_PROFILES_ACTIVE=prod on the server.
# Production overrides live in application-prod.properties.
# ============================================================
spring.datasource.url=jdbc:h2:file:./data/blogapp
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=admin
spring.datasource.password=password
spring.jpa.open-in-view=false
server.error.whitelabel.enabled=false

# H2 Console (dev only - disabled in prod profile)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.jpa.hibernate.ddl-auto=update

# File upload limits (50 MB)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=55MB

# Upload storage
app.upload.dir=./uploads
app.upload.max-bytes=52428800

# Initial admin account - must be set as environment variables
app.seed.admin.email=${SEED_ADMIN_EMAIL}
app.seed.admin.password=${SEED_ADMIN_PASSWORD}
app.seed.admin.firstName=${SEED_ADMIN_FIRST_NAME}
app.seed.admin.lastName=${SEED_ADMIN_LAST_NAME}

